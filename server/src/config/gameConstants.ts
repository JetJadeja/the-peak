// Spawn Configuration
export const SPAWN_AREA_SIZE = 40; // Players spawn within this area
export const SPAWN_HEIGHT = 0.5; // Height above ground
export const SPAWN_MIN_DISTANCE = 5; // Minimum distance between spawn points

// Player Configuration
export const DEFAULT_PLAYER_ROTATION = { x: 0, y: 0, z: 0 };
export const DEFAULT_PLAYER_VELOCITY = { x: 0, y: 0, z: 0 };
export const DEFAULT_PLAYER_COLOR = '#DEAF1A'; // Default yellow color
